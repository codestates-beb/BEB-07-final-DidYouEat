import { getUser } from "./getUser";
import { createUser } from "./createUser";
import { getAllToken } from "./getAllToken";

const userUtil = {
  getUser,
  createUser,
  getAllToken,
};

export { userUtil };
